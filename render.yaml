services:
  - type: web
    name: craftopia-api
    runtime: node
    # CRITICAL: Root directory must be "." (the repository root), NOT "src"
    rootDir: .
    buildCommand: npm ci && npm run build && ls -la dist/
    startCommand: node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
